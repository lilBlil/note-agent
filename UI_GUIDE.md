# Note Agent UI 使用指南

全新 ChatGPT / Notion 风格界面，单屏工作区，实时展示 AI Research Agent 执行过程。

---

## 启动应用

```bash
# 开发模式（热重载）
uv run streamlit run app.py

# 生产模式
uv run note-agent-ui
```

浏览器自动打开 `http://localhost:8501`

---

## 界面布局

### 左侧 Sidebar（可收起）

#### 历史记录
- 🟢 成功任务
- 🔴 失败任务  
- 🟡 运行中任务

点击可重新查看完整笔记、来源和元数据。

点击 **＋ 新研究** 返回主界面。

#### 设置
- **LLM 模型：** deepseek / openai / qwen / moonshot / zhipu / siliconflow
- **Search Provider：** duckduckgo / tavily / perplexity / searxng
- **Iteration 设置：**
  - 固定流程：硬上限（达到后强制最终化）
  - ReAct 自主：软预算（Agent 自主决定是否继续）
- **Notion 发布：** 展开勾选，完成后自动发布
- **高级参数：** 生成多模态资产（公式/代码/图表/流程图）

---

### 主工作区

#### 用户任务卡片（顶部）
提交后固定显示：
- 输入的文本主题
- 上传的文件名（📎 标记）
- 粘贴的 URL（🔗 标记）
- 当前研究模式（固定流程 / ReAct 自主）

#### Agent Status（左侧 25%，独立滚动）

**固定流程模式：**
```
固定流程  ·  ● 运行中
Iteration 1 / 3

✓ 任务分析
✓ 生成初稿
✓ 查询生成
● 信息检索
○ 内容验证
○ 生成笔记
```

**ReAct 自主模式：**
```
ReAct 自主  ·  ● 运行中
Iteration 2

[Iteration 1]
  Think: Agent 分析需求
  Act: 🧭 推断结构
  Observe: 确定为技术笔记

[Iteration 2]
  Think: Agent 决策：检索 / 修正 / 最终化
  Act: 📚 检索资料
  Observe: 获得新的参考来源
```

#### 生成内容 Output（右侧 75%，独立滚动）

- **固定流程：** 实时流式显示正在生成的文本
- **ReAct 自主：** Agent 决策完成后一次性呈现最终笔记
- 支持 Markdown、代码块、LaTeX 公式、Mermaid 流程图

#### 详细信息（折叠面板）

点击 **▼ 详细信息** 展开：

- **Sources：** 引用的网页、论文、学术资料 URL
- **Token Usage：** 总计 tokens（输入/输出），按节点分解
- **Execution Trace：** 完整执行日志（节点标签、工具调用、警告）

底部显示 Run ID 和日志目录路径。

---

### 研究模式 + 输入框（底部固定）

#### 研究模式选择（分段按钮）
- **固定流程：** 预定义管道（分析 → 初稿 → 查询 → 检索 → 验证 → 最终化）
- **ReAct 自主：** Agent 根据当前状态自主选择工具调用

#### 输入框（ChatGPT 风格）
```
┌────────────────────────────────────────────────────┐
│  ＋   输入主题、粘贴文本，或添加文件与网站…     ↑  │
└────────────────────────────────────────────────────┘
```

- **文本输入：** 直接输入主题或粘贴长文
- **URL 自动识别：** 粘贴 `https://...` 会自动提取并抓取内容
- **文件上传：** 点击 ＋ 上传 `.txt` 或 `.md` 文件
- **混合输入：** 可同时输入文本 + 上传文件 + 粘贴 URL

点击 ↑ 或按回车发送。

---

## 典型工作流

### 1. 快速研究（纯文本）

1. 在输入框输入：`Python 装饰器的工作原理和应用场景`
2. 保持默认设置（固定流程，1 次迭代）
3. 点击发送 ↑
4. 左侧观察 Agent 状态：分析 → 初稿 → 查询 → 检索 → 验证 → 生成
5. 右侧实时看到笔记逐步生成
6. 完成后查看详细信息中的引用来源

### 2. 深度研究（多次迭代 + 网页）

1. 粘贴：`LangGraph 工作流设计 https://langchain.com/docs/langgraph`
2. 设置 Iteration = 3（允许 Agent 多次检索和修正）
3. 发送后，Agent 会：
   - 抓取粘贴的 URL 内容
   - 生成初稿
   - 识别信息缺口
   - 自动检索补充资料
   - 核验并修正（最多 3 轮）
   - 生成最终笔记
4. 展开详细信息查看所有引用来源和 Token 用量

### 3. ReAct 自主模式

1. 切换到 **ReAct 自主**
2. 输入：`对比 PyTorch 和 TensorFlow 的优劣`
3. Agent 会自主决定：
   - 先推断笔记结构
   - 生成初稿
   - 判断是否需要检索（Think → Act → Observe）
   - 决定是否继续修正或直接最终化
4. 左侧面板展示每次决策的 Think / Act / Observe

### 4. 文件 + 网页混合输入

1. 准备一个 `notes.md` 文件（本地笔记草稿）
2. 点击 ＋ 上传文件
3. 同时在文本框输入：`基于附件内容，结合 https://arxiv.org/... 论文，生成综述`
4. Agent 会整合：
   - 你的草稿内容
   - 指定论文的内容
   - 自动检索的补充资料

### 5. 查看历史任务

1. 左侧 Sidebar 历史记录区自动列出所有任务
2. 点击任意任务（带状态点 🟢🔴🟡）
3. 右侧立即展示该任务的完整笔记和来源
4. 只读模式，不会重新执行
5. 点击 **＋ 新研究** 返回主界面

---

## 高级功能

### 多模态资产生成

1. 展开 Sidebar → **高级参数**
2. 勾选 **生成多模态资产**
3. Agent 会自动识别是否需要：
   - LaTeX 公式（内联 / 块级）
   - 代码示例（带语法高亮）
   - Mermaid 流程图（自动渲染为图形）
   - 表格和列表
4. 生成的资产直接嵌入 Markdown 笔记
5. 详细信息中显示生成的资产文件路径

### Notion 自动发布

1. 展开 Sidebar → **Notion 发布**
2. 勾选 **发布到 Notion**
3. 确保已配置 Notion API（参考主 README）
4. 完成后，详细信息中会显示 Notion 页面链接
5. 点击直接跳转到 Notion 查看

---

## 状态指示

- **⚪ 待处理：** 节点还未开始
- **● 运行中：** 节点正在执行（黄色）
- **✓ 完成：** 节点已完成（绿色）
- **● 出错：** 执行失败（红色）

---

## 常见场景

| 场景 | 推荐设置 |
|------|---------|
| 快速概述 | 固定流程，Iteration 0 或 1 |
| 深度研究 | 固定流程，Iteration 2-3 |
| 探索式研究 | ReAct 自主，Iteration 预算 2-3 |
| 已有资料整合 | 上传文件 + 固定流程 |
| 论文综述 | 粘贴 arXiv URL + 多次迭代 |
| 技术文档生成 | 开启多模态资产 |

---

## 快捷键

- **回车：** 发送输入（等同点击 ↑）
- **ESC：** （Streamlit 原生）收起 Sidebar

---

## 视觉主题

界面采用深色低饱和设计：
- **背景：** 深灰黑（#111214）
- **面板：** 层叠灰色（#191a1d / #1e2024）
- **边框：** 中性灰（#2a2c31）
- **文本：** 浅灰白（#e6e7ea）
- **状态色：** 仅用于指示成功/运行/错误
- **强调色：** 克制的蓝色（#6b8afd）

圆角、留白、清晰层级 = 专业且舒适的长时间工作体验。

---

## 疑难排查

**输入框没有反应？**
- 确保至少输入了文本、上传了文件，或粘贴了 URL
- 空输入不会触发提交

**历史任务加载失败？**
- 检查 `runs/` 目录是否完整
- 部分旧任务可能缺少 `final_state.json`

**ReAct 模式一直显示"生成中"？**
- ReAct Agent 不流式输出，需等待最终化完成
- 查看左侧状态面板确认是否还在执行

**页面滚动不流畅？**
- 状态和输出区域是独立滚动容器
- 不要滚动整个页面，在对应区域内滚动

**网页抓取失败？**
- 某些网站有反爬限制
- 查看详细信息 → Execution Trace 确认错误
- 尝试手动复制内容后粘贴到输入框

---

**享受你的 AI Research Agent 工作空间！** 🚀
